name: nemotron_bot
description: A minimal local LLM chat CLI built on llamadart.
version: 0.1.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.11.5

# Add regular dependencies here.
dependencies:
  llamadart: ^0.8.17
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
